---
interface Props {
	title: string;
	body: string;
}

const { title, body } = Astro.props;
---
<article class="my-16">
    <slot name="before"/>
    <section>
        <h2 class="font-extrabold text-3xl">
            {title}
        </h2>
        <p class="text-base text-justify">
            {body}
        </p>
    </section>
    <slot name="after"/>
</article>